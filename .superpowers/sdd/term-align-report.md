# 术语对齐报告：Markdown 全文 → 纯文本全文

**日期**：2026-07-30
**范围**：纯文档。仅 `任务文档/01-Python文档工具.md` 与 `任务文档/概念速查.md`
**规则**：pymupdf 输出的是**纯文本**，不是结构化 Markdown。
- 描述「PDF 提取出的文本内容」的句子 → 改为「纯文本（全文）」。
- 显式命名 MongoDB `markdown` 字段 / 参数 / 字段结构 `{..., markdown, ...}` → **保留**「markdown」（它是字段名，不是格式声明）。
- 指向其他工具（marker-pdf、MinerU）产出的「结构化 Markdown」格式 → **保留**（确实指 Markdown 格式）。

已对齐的参照风格（来自此前已完成的 03、项目概览）：`纯文本全文`、`完整纯文本全文`、`一篇论文的纯文本全文`。

---

## 文件 1：`任务文档/01-Python文档工具.md`

| # | 原行号 | 决策 | 原文片段 → 改后片段 |
|---|--------|------|---------------------|
| 1 | L17 | **改** | `Agent 读取本模块产出的 Markdown 全文` → `Agent 读取本模块产出的纯文本全文` |
| 2 | L60 | **改** | `documents 集合存放论文 Markdown 全文` → `documents 集合存放论文纯文本全文` |
| 3 | L191 | **改** | `# MongoDB Python 驱动（同步）：存取 Markdown 全文与记忆` → `# ...存取纯文本全文与记忆`（注释对齐空格已保持） |
| 4 | L336 | **改** | 表格描述 `把 Markdown 全文写入 MongoDB documents 集合` → `把纯文本全文写入 MongoDB documents 集合`（签名参数 `markdown` 保留） |
| 5 | L338 | **改** | 表格描述 `从 MongoDB 读取指定文档的 Markdown 全文` → `从 MongoDB 读取指定文档的纯文本全文` |
| 6 | L426 | **改**（两处） | `把 PDF 转成 Markdown 文本` → `把 PDF 转成纯文本`；`转换后的完整 Markdown 全文` → `转换后的完整纯文本全文`（行内字段结构 `{..., markdown, ...}` 保留） |
| 7 | L446 | **改** | docstring `把一篇 Markdown 全文存入 documents 集合` → `把一篇纯文本全文存入 documents 集合` |
| 8 | L486 | **改** | docstring `按 doc_id 读取一篇文档的 Markdown 全文` → `按 doc_id 读取一篇文档的纯文本全文` |
| 9 | L757 | **改** | `read_document 读取论文 Markdown 全文` → `read_document 读取论文纯文本全文` |

## 文件 2：`任务文档/概念速查.md`

| # | 原行号 | 决策 | 原文片段 → 改后片段 |
|---|--------|------|---------------------|
| 10 | L174 | **改** | `L1/L2 记忆与论文 Markdown 全文` → `L1/L2 记忆与论文纯文本全文`（同行 `PDF、Markdown、JSON 散落` 是泛指文件类型，保留） |
| 11 | L176 | **改** | `documents（论文 Markdown，文档结构 {...}）` → `documents（论文纯文本，文档结构 {...}）`（花括号内字段名 `markdown` 保留） |
| 12 | L188 | **改** | `上传的 Markdown 全文是否完整` → `上传的纯文本全文是否完整` |
| 13 | L229 | **改** | `read_document() ... 取回单篇 Markdown 全文` → `read_document() ... 取回单篇纯文本全文` |
| 14 | L377 | **改** | `（L1/L2 记忆、论文 Markdown 全文）` → `（L1/L2 记忆、论文纯文本全文）` |
| 15 | L388 | **改** | `└── documents # 文档区：完整 Markdown 全文` → `└── documents # 文档区：完整纯文本全文`（下一行字段名 `markdown` 保留） |

---

## 保留项（合法，非改动范围）

以下 `Markdown`/`markdown` 出现均按规则**保留**，无需改动：
- **字段名 / 参数名 / 变量名** `markdown`：L336(签名)、L424/426/445/451/464/470/471/490/615/664（01）；L162/176/190/381/389（概念速查）；L640/641/645 代码变量（01）。
- **结构化 Markdown 格式**（指 marker-pdf/MinerU 等）：01-L56、01-L382（"无 Markdown 表格重建"）、概念速查-L160。
- **测试函数名** `test_read_document_returns_markdown`：01-L260 无对应、概念速查-L260（标识符，未在本次范围）。

---

## 存疑 / 边界项（未在显式清单内，按"拿不准就保留并报告"原则保留原样）

这几处同样是"描述提取内容"的措辞，按规则**可**改为纯文本，但**不在本次明确的 15 行清单**内，故保留原样、提交主控决策：

1. **01-L259**：`本模块的文档服务（存取 Markdown）` —— 括注描述文档服务存取的内容，与同文件 L60/L426 的措辞不一致。建议若需一致改为 `（存取纯文本）`。
2. **01-L584**：测试 docstring `"""存入后应能按 doc_id 读回完整 Markdown。"""` —— 描述读回的内容。建议改为 `读回完整纯文本`。
3. **概念速查-L178**：`卡上有文件名、全文 Markdown、上传时间` —— 描述卡片内容。建议改为 `全文纯文本`。
4. **概念速查-L379**：`上传的 PDF、转换出的 Markdown、积累的记忆` —— 描述转换产物。建议改为 `转换出的纯文本`。

> 以上 4 处未自行修改，等待主控确认是否纳入本次或后续清扫。

---

**已应用改动数**：15 行（文件 1 共 9 行，文件 2 共 6 行；其中 L426 单行两处）。
