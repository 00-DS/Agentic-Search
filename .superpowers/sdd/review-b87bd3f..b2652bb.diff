diff --git a/任务文档/00-开始指南.md b/任务文档/00-开始指南.md
index 002d4f4..315dde3 100644
--- a/任务文档/00-开始指南.md
+++ b/任务文档/00-开始指南.md
@@ -5,42 +5,42 @@
 这是一套面向初学者的完整开发教程。无需前端框架、容器化或 AI Agent 框架经验——按照文档逐步操作，即可从零构建一个「带记忆的论文问答助手」。
 
 本项目的架构刻意保持极简：**一个 Python 后端服务 + 两个原生 HTML/JS 前端文件，零构建步骤**。
 
 ## 项目简介
 
 **Agentic Search with Memory**：
 
 1. 用户在浏览器中上传论文 PDF
 2. 输入问题（如「这篇论文的核心方法是什么」）
-3. AI 自主探索论文（先翻目录、再读相关章节），展示推理过程，流式输出回答
+3. AI 自主探索论文（定位关键段落、按需读取），展示推理过程，流式输出回答
 4. AI 利用 TMT 记忆系统记住用户的研究方向，后续对话中引用历史
 
 ## 你将学到什么
 
 | 技术 | 你会用它做什么 | 在哪里学 |
 |------|-------------|---------|
-| Python | 文档处理工具（PDF → 章节文本）+ pytest 测试 | [模块 1](./01-Python文档工具.md) |
+| Python | 文档处理工具（PDF → 纯文本）+ pytest 测试 | [模块 1](./01-Python文档工具.md) |
 | LangGraph + FastAPI | ReAct agent（工具调用 + 条件边）+ HTTP API 服务 | [模块 2](./02-LangGraph-Agent.md) |
 | 原生 HTML + JavaScript | 前端界面 + Agent 推理的流式可视化（fetch + DOM） | [模块 3](./03-HTML前端.md) |
 | TMT 记忆系统 | L1 事实提取 + L2 会话摘要（前端按钮手动整合） | [模块 4](./04-TMT记忆系统.md) |
 
 ## 学习路径
 
 ```
 模块 1 → 模块 2 → 模块 3 → 模块 4
 ```
 
 推荐顺序的依据：
 
-1. **模块 1（Python 文档工具）** — 先实现最底层的能力：把 PDF 切成可读的章节文本。这是整个系统的地基，后续所有模块都依赖它。
-2. **模块 2（LangGraph Agent）** — 在文档能力之上构建「大脑」：用 LangGraph 搭一个 **ReAct agent**——LLM 自主调 `list_papers`/`list_sections`/`read_section`/`search_sections` 等工具探索论文，读哪篇、读哪段都由它当场决定，并用 FastAPI 把它暴露为 HTTP API。
+1. **模块 1（Python 文档工具）** — 先实现最底层的能力：把 PDF 转成完整纯文本。这是整个系统的地基，后续所有模块都依赖它。
+2. **模块 2（LangGraph Agent）** — 在文档能力之上构建「大脑」：用 LangGraph 搭一个 **ReAct agent**——LLM 自主调 `list_papers`/`read_paper`/`search_papers`/`extract_abstract` 等工具探索论文，读哪篇、读哪段都由它当场决定，并用 FastAPI 把它暴露为 HTTP API。
 3. **模块 3（HTML 前端）** — 大脑就绪后，再做让用户交互的界面：原生 HTML + fetch 调用上面的 API，看到流式回答。
 4. **模块 4（TMT 记忆系统）** — 最后给 Agent 装上「记忆」：L1 每轮自动提取事实，L2 由前端按钮手动触发整合。
 
 ## 开始之前
 
 ### 你需要准备的
 
 1. **一台电脑**：Windows / macOS / Linux 均可
 2. **命令行基础**：会 cd、ls（或 dir）即可
 3. **一个现代浏览器**：Chrome / Edge / Firefox 任一，用于运行前端
diff --git a/任务文档/概念速查.md b/任务文档/概念速查.md
index 4d8e1c9..7f0f335 100644
--- a/任务文档/概念速查.md
+++ b/任务文档/概念速查.md
@@ -1,23 +1,23 @@
 # 概念速查
 
 英文术语按字母排列，中文术语列于文末。每个条目按「定义 → 为什么需要 → 本项目用法 → 理解示例 → 延伸阅读」五段式展开，便于初学者既知其然又知其所以然。遇到不理解的术语随时查阅；更详细的说明见各模块的「技术概念」段落。
 
 ---
 
 ## Agentic Search
 
 **定义**：由 Agent（智能体）在运行时自主决定下一步动作的文档处理模式。Agent 把「读什么、怎么读、如何回答」交给 LLM 按问题特征实时判断。
 
-**为什么需要**：不同问题所需的阅读深度与范围截然不同——「这篇论文用了什么数据集」只需扫一眼实验章节，「复现实验要装哪些依赖」却要交叉比对方法与附录。Agent 模式不预先把全文塞进上下文，而是让 LLM 自主决定读哪篇、读哪段：先看目录（`list_sections`），再按需取某一章（`read_section`），或在全部章节里正则定位关键词（`search_sections`），证据够了就直接作答。
+**为什么需要**：不同问题所需的阅读深度与范围截然不同——「这篇论文用了什么数据集」只需扫一眼实验章节，「复现实验要装哪些依赖」却要交叉比对方法与附录。Agent 模式不预先把全文塞进上下文，而是让 LLM 自主决定读哪篇、读哪段：先用正则定位关键词行号（`search_papers`），再按行号取片段（`read_paper`），或提取摘要判断相关性（`extract_abstract`），证据够了就直接作答。
 
-**本项目用法**：Agent 工作流定义在 `backend/src/agentic_search/agents/graph.py`，由 LangGraph 编排为一个 **ReAct 循环**（详见 ReAct 条目）：`llm_call`（LLM 决策）与 `tool_node`（执行工具）之间靠条件边 `should_continue` 往返，直到 LLM 不再调工具、给出最终答案。四个论文导航工具——`list_papers`（看有哪些论文）、`list_sections`（看某篇的目录）、`read_section`（取某一章正文）、`search_sections`（正则跨章节定位）——调用顺序完全由 LLM 在运行时决定。模块 4 在此循环外层加上 `retrieve_memory`（进入前注入历史）与 `store_memory`（结束后提取事实）。
+**本项目用法**：Agent 工作流定义在 `backend/src/agentic_search/agents/graph.py`，由 LangGraph 编排为一个 **ReAct 循环**（详见 ReAct 条目）：`llm_call`（LLM 决策）与 `tool_node`（执行工具）之间靠条件边 `should_continue` 往返，直到 LLM 不再调工具、给出最终答案。四个论文导航工具——`list_papers`（看有哪些论文）、`read_paper`（按行号取片段）、`search_papers`（正则定位行号）、`extract_abstract`（提取摘要）——调用顺序完全由 LLM 在运行时决定。模块 4 在此循环外层加上 `retrieve_memory`（进入前注入历史）与 `store_memory`（结束后提取事实）。
 
 **理解示例**（教学示例）：Agentic Search 像有判断力的研究员——不一口气通读全部论文，而是先翻目录、挑相关章节精读、必要时换几个关键词再搜一遍，觉得证据够了就动笔作答。它对标 omp、Claude Code 用 `glob`/`read`/`grep` 自主探索代码库的能力，对象从代码换成了论文。
 
 **延伸阅读**：[LangGraph · Agentic Concepts](https://langgraph.com.cn/concepts/agentic_concepts/)
 
 ---
 
 ## AJAX
 
 **定义**：Asynchronous JavaScript and XML 的缩写，指浏览器在不刷新整个页面的前提下，异步向服务器请求数据或发送数据的技术模式的统称。它不是某个具体库，而是「异步获取数据、局部更新界面」这一思想的统称。
@@ -188,53 +188,53 @@ graph.add_edge("tools", "llm_call")            # 工具结果回灌 → 继续 l
 app = graph.compile()                           # 编译成可运行对象
 ```
 何时结束由 LLM 决定（不再返回 `tool_calls` 即终止），而不是写死在第几步。
 
 **延伸阅读**：[LangGraph 官方文档](https://langgraph.com.cn/)
 
 ---
 
 ## pymupdf
 
-**定义**：基于 MuPDF 内核的轻量 PDF 处理库。默认的 `page.get_text()` 返回无结构的纯文字；本项目用的是 `page.get_text("dict")`，它返回「块 → 行 → 字符块（span）」四级层级，每个字符块带字号、字体、粗体等属性——据此可用标题字号启发式切出章节边界。单一 wheel、无模型下载、无 GPU 依赖。
+**定义**：基于 MuPDF 内核的轻量 PDF 处理库。`page.get_text("text")` 返回该页的纯文本字符串——单一 wheel、无模型下载、无 GPU 依赖、即装即用。
 
-**为什么需要**：Agent 要按章节探索论文（看目录、读某一章、跨章搜关键词），需要先把 PDF 转成结构化的章节文本，而不是一整段无标记的全文。pymupdf 用最简的方式做到：即装即用、零额外依赖。旧的 `get_text("text")` 只返回无结构的纯文本，agent 无法按章取片段；改用 `get_text("dict")` 拿到带字号的字符块层级后，按标题字号即可切出 `{section_id, title, level, text}` 章节数组。
+**为什么需要**：Agent 要按行号自主探索论文（正则定位、按行号取片段），需要先把 PDF 转成纯文本。pymupdf 用最简的方式做到：即装即用、零额外依赖。不做任何切分——切分是 agent 的职责，它用正则搜索定位行号、按行号读取片段。
 
-**本项目用法**：在 `backend/src/agentic_search/services/documents.py` 的 `parse_pdf()` 函数中调用 pymupdf 的 `get_text("dict")`，把 PDF 切成章节数组后连同 `doc_id`、`filename`、`uploaded_at` 存入 MongoDB 的 `documents` 集合（`sections` 字段保存章节数组，不再存单一全文字段），原 PDF 不落盘、不保留任何文件。Agent 经 `read_section(doc_id, section_id)` 只取需要的某一章，或经 `read_document(doc_id)` 把各章拼成全文。
+**本项目用法**：在 `backend/src/agentic_search/services/documents.py` 的 `parse_pdf()` 函数中调用 pymupdf 的 `get_text("text")`，把 PDF 转为完整纯文本后连同 `doc_id`、`filename`、`uploaded_at` 存入 MongoDB 的 `documents` 集合。Agent 经 `read_paper(doc_id, start_line, end_line)` 按行号取片段，或经 `search_papers(pattern)` 正则定位。
 
-**理解示例**（教学示例）：一份多页论文 PDF，pymupdf 逐页调用 `get_text("dict")` 拿到带字号字体的字符块层级，按标题字号切出「1 引言」「2 方法」「3 实验」等章节，每章一段 `text`——agent 想看实验就只取第 3 章，而不必把全文塞进上下文。
+**理解示例**（教学示例）：一份多页论文 PDF，pymupdf 逐页调用 `get_text("text")` 提取纯文本，所有页拼接成一个完整字符串存入 MongoDB——agent 想看实验就用 `search_papers("experiment|evaluation")` 定位行号，再用 `read_paper` 取那段文本。
 
 **延伸阅读**：[pymupdf 官方文档](https://pymupdf.readthedocs.io/)
 
 ---
 
 ## MongoDB
 
 **定义**：一种文档型（document-oriented）NoSQL 数据库。数据以「文档」（document，结构类似 JSON 对象）为单位存储，相关文档归入同一个「集合」（collection，类似关系库的表），多个集合组成一个「数据库」（database）。
 
-**为什么需要**：本项目有两类数据需要持久化——L1/L2 记忆与论文章节正文。MongoDB 以灵活的文档结构存储，字段可增可减、无需建表迁移，且官方提供成熟的 Python 驱动（PyMongo）与可视化工具（Compass），适合教学项目的快速迭代。
+**为什么需要**：本项目有两类数据需要持久化——L1/L2 记忆与论文文本。MongoDB 以灵活的文档结构存储，字段可增可减、无需建表迁移，且官方提供成熟的 Python 驱动（PyMongo）与可视化工具（Compass），适合教学项目的快速迭代。
 
-**本项目用法**：本地运行一个 MongoDB 实例（默认地址 `localhost:27017`），所有数据集中存于名为 `agentic_search` 的数据库。该库下有两个集合：`memories`（记忆，文档结构 `{ _id, level, content, timestamp, session_id }`）与 `documents`（论文章节，文档结构 `{ _id, doc_id, filename, sections, uploaded_at }`，`sections` 是 `parse_pdf` 切好的 `{section_id, title, level, text}` 章节数组）。连接配置 `mongo_uri` 与 `mongo_db` 定义在 `configs/config.py`，后端通过 PyMongo 读写这两个集合；`documents` 现主要供 agent 的论文导航工具读取，前端不再渲染文档列表。
+**本项目用法**：本地运行一个 MongoDB 实例（默认地址 `localhost:27017`），所有数据集中存于名为 `agentic_search` 的数据库。该库下有两个集合：`memories`（记忆，文档结构 `{ _id, level, content, timestamp, session_id }`）与 `documents`（论文文本，文档结构 `{ _id, doc_id, filename, text, uploaded_at }`，`text` 是 `parse_pdf` 提取的完整纯文本）。连接配置 `mongo_uri` 与 `mongo_db` 定义在 `configs/config.py`，后端通过 PyMongo 读写这两个集合；`documents` 现主要供 agent 的论文导航工具读取，前端不渲染文档列表。
 
-**理解示例**（教学示例）：文档结构像一张可随手加字段的「登记卡」——`documents` 集合里每张卡记录一篇论文（卡上有文件名、章节正文数组、上传时间），`memories` 集合里每张卡记录一条记忆（卡上有级别 L1/L2、内容、时间、会话 ID）。集合就是把同类卡归档的抽屉，数据库就是存放所有抽屉的档案柜。
+**理解示例**（教学示例）：文档结构像一张可随手加字段的「登记卡」——`documents` 集合里每张卡记录一篇论文（卡上有文件名、完整文本、上传时间），`memories` 集合里每张卡记录一条记忆（卡上有级别 L1/L2、内容、时间、会话 ID）。集合就是把同类卡归档的抽屉，数据库就是存放所有抽屉的档案柜。
 
 **延伸阅读**：[MongoDB 官方文档（数据库与集合）](https://www.mongodb.com/zh-cn/docs/manual/core/databases-and-collections/)
 
 ---
 
 ## MongoDB Compass
 
 **定义**：MongoDB 官方提供的图形化客户端（GUI）。它以可视化界面连接到 MongoDB 实例，让用户无需手写查询语句即可浏览数据库、集合、文档的内容，也能执行增删改查与查看索引。
 
-**为什么需要**：Compass 提供「点开数据库 → 点开集合 → 看到一条条文档」的树状浏览体验，相当于「数据库版的文件管理器」，便于在教学调试时直观确认「记忆是否写进去了」「PDF 切出的章节是否完整」。
+**为什么需要**：Compass 提供「点开数据库 → 点开集合 → 看到一条条文档」的树状浏览体验，相当于「数据库版的文件管理器」，便于在教学调试时直观确认「记忆是否写进去了」「PDF 提取的文本是否完整」。
 
-**本项目用法**：学生本地安装 Compass 后，连接到 `mongodb://localhost:27017`，即可在左侧目录树中看到 `agentic_search` 数据库，展开后看到 `memories` 与 `documents` 两个集合，点击任一集合即可逐条查看文档。例如上传一篇 PDF 后，可在 `documents` 集合里看到对应的 `{ doc_id, filename, sections, uploaded_at }` 文档（`sections` 是章节数组，每项含 `section_id`/`title`/`level`/`text`）；对话并整合记忆后，可在 `memories` 集合里看到 L1/L2 文档。它是验证「数据确实落库」的首选工具。
+**本项目用法**：学生本地安装 Compass 后，连接到 `mongodb://localhost:27017`，即可在左侧目录树中看到 `agentic_search` 数据库，展开后看到 `memories` 与 `documents` 两个集合，点击任一集合即可逐条查看文档。例如上传一篇 PDF 后，可在 `documents` 集合里看到对应的 `{ doc_id, filename, text, uploaded_at }` 文档（`text` 是完整纯文本）；对话并整合记忆后，可在 `memories` 集合里看到 L1/L2 文档。它是验证「数据确实落库」的首选工具。
 
 **理解示例**（教学示例）：PyMongo 是「用代码与数据库对话」，Compass 是「用眼睛看数据库里有什么」——前者面向程序、后者面向人，二者互补，调试时先用 Compass 看现象、再用 PyMongo 改逻辑。
 
 **延伸阅读**：[MongoDB Compass 官方文档](https://www.mongodb.com/zh-cn/docs/compass/current/)
 
 ---
 
 ## Pydantic
 
 **定义**：Python 的数据验证与序列化库，FastAPI 的内置依赖。开发者用继承 `BaseModel` 的类声明数据结构（如请求体、响应体），Pydantic 自动完成类型校验、错误提示与 JSON ↔ Python 对象的互转。
@@ -258,21 +258,21 @@ q = Query(**{"doc_id": "abc"})                            # 自动报错：缺 q
 **延伸阅读**：[Pydantic 官方文档](https://pydantic.com.cn/)
 
 ---
 
 ## PyMongo
 
 **定义**：MongoDB 官方维护的 Python 驱动（driver）库。它把「连接数据库、插入文档、查询、更新、删除」等操作封装成 Python 方法，让后端代码以 Python 对象的方式操作 MongoDB，而无需手写底层网络协议或查询字符串。
 
 **为什么需要**：应用程序要通过网络与 MongoDB 通信，PyMongo 把通信协议（Wire Protocol）细节全部封装，开发者只需 `db.collection.insert_one({...})` 这样直观的方法调用即可完成读写。本项目用同步驱动：教学项目数据量小，阻塞开销可接受，API 简单直观、文档丰富，利于初学者上手。
 
-**本项目用法**：后端 `memory/store.py` 与 `services/documents.py` 通过 PyMongo 操作 MongoDB。连接初始化在模块加载时完成（`MongoClient(mongo_uri)` 取得客户端，再按 `mongo_db` 取得数据库对象），随后各函数对 `memories`、`documents` 集合做增删改查。例如 `save_memory()` 用 `insert_one()` 写入一条记忆，`load_memories()` 用 `find()` 按条件查询，`read_document()` 用 `find_one()` 按 `doc_id` 取回一篇论文（`sections` 章节数组），`read_section()` 则从该数组取指定 `section_id` 的一章。
+**本项目用法**：后端 `memory/store.py` 与 `services/documents.py` 通过 PyMongo 操作 MongoDB。连接初始化在模块加载时完成（`MongoClient(mongo_uri)` 取得客户端，再按 `mongo_db` 取得数据库对象），随后各函数对 `memories`、`documents` 集合做增删改查。例如 `save_memory()` 用 `insert_one()` 写入一条记忆，`load_memories()` 用 `find()` 按条件查询，`read_document()` 用 `find_one()` 按 `doc_id` 取回一篇论文（`text` 完整纯文本）。
 
 **理解示例**（教学示例）：
 ```python
 # 教学示例：插入一条与查询多条
 from pymongo import MongoClient
 
 client = MongoClient("mongodb://localhost:27017")
 db = client["agentic_search"]
 
 db.memories.insert_one({"level": "L1", "content": "用户关注实验复现", "session_id": "s1"})  # 插入一条
@@ -306,23 +306,23 @@ def test_read_document_returns_markdown():
 **延伸阅读**：[pytest 官方文档](https://pytest.cn/)
 
 ---
 
 ## ReAct
 
 **定义**：Reasoning + Acting，LLM + 工具的循环工作模式。LLM 先「思考」该做什么，再「行动」调用一个工具，拿到结果后继续思考，直到认为信息足够、直接给出最终答案。它是「agentic」的核心范式。
 
 **为什么需要**：把「先做 A 再做 B」写死成固定流程，无法应对千变万化的问题——有的问题扫一眼摘要就够了，有的要交叉读三章。ReAct 把「下一步做什么」交给 LLM 在运行时按问题特征实时判断，路径完全动态。它对标 omp、Claude Code 用 `glob`/`read`/`grep` 自主探索代码库的能力——本项目的 agent 把这套能力换到了论文上。
 
-**本项目用法**：`agents/graph.py` 用 LangGraph 把 ReAct 实现为两个节点 + 一条条件边——`llm_call`（LLM 决策）与 `tool_node`（执行工具）靠条件边 `should_continue` 往返，`should_continue` 看到 `tool_calls` 就继续循环，否则终止。运转靠**工具调用协议（tool calling）**：`bind_tools` 把四个论文导航工具（`list_papers`/`list_sections`/`read_section`/`search_sections`）的 schema 绑给 LLM，LLM 在响应里返回结构化的 `tool_calls`（函数名 + 参数 JSON），`ToolNode` 解析并执行、把结果包成 `ToolMessage` 回灌给 LLM。注意 `search_sections` 用正则、不用 embedding——智能来自 LLM 自主迭代构造正则，而非预计算的向量相似度。
+**本项目用法**：`agents/graph.py` 用 LangGraph 把 ReAct 实现为两个节点 + 一条条件边——`llm_call`（LLM 决策）与 `tool_node`（执行工具）靠条件边 `should_continue` 往返，`should_continue` 看到 `tool_calls` 就继续循环，否则终止。运转靠**工具调用协议（tool calling）**：`bind_tools` 把四个论文导航工具（`list_papers`/`read_paper`/`search_papers`/`extract_abstract`）的 schema 绑给 LLM，LLM 在响应里返回结构化的 `tool_calls`（函数名 + 参数 JSON），`ToolNode` 解析并执行、把结果包成 `ToolMessage` 回灌给 LLM。注意 `search_papers` 用正则、不用 embedding——智能来自 LLM 自主迭代构造正则，而非预计算的向量相似度。
 
-**理解示例**（教学示例）：问「这篇论文用了什么数据集」，agent 的典型轨迹是 `list_papers` → `list_sections` → `search_sections("dataset|corpus|benchmark")` → 在命中章节用 `read_section` 取正文 → 给出答案；换一个更窄的问题，它可能两步就收尾。走几步、调哪些工具，由 LLM 当场决定。
+**理解示例**（教学示例）：问「这篇论文用了什么数据集」，agent 的典型轨迹是 `list_papers` → `extract_abstract`（判断相关性）→ `search_papers("dataset|corpus|benchmark")` → 在命中行号用 `read_paper` 取正文 → 给出答案；换一个更窄的问题，它可能两步就收尾。走几步、调哪些工具，由 LLM 当场决定。
 
 **延伸阅读**：[LangGraph · Agentic Concepts](https://langchain-ai.github.io/langgraph/concepts/agentic_concepts/)、[模块 2](./02-LangGraph-Agent.md)
 
 ---
 
 ## ReadableStream
 
 **定义**：浏览器原生的流式数据读取接口，代表一个可分块（chunk）逐步读取的数据源，而非一次性返回全部内容的整体。
 
 **为什么需要**：对一份大响应（如 LLM 逐字生成的长回答），若等全部生成完再一次性返回，用户要盯着空白屏幕干等数秒。流式接口允许「生产一点、消费一点」：服务端边生成边推、客户端边收边显示，从而实现打字机式的实时输出。ReadableStream 就是客户端逐块取出这些数据的把手。
@@ -420,27 +420,27 @@ uv run uvicorn agentic_search.main:app   # 在装好的环境里启动服务
 **本项目用法**：包名为 `agentic_search`，源码位于 `backend/src/agentic_search/`（其下含 `main.py`、`configs/`、`api/`、`agents/`、`memory/`、`services/`）。执行 `uv sync`（或 `pip install -e .`）后，即可在任意位置用 `from agentic_search.agents.graph import ...` 形式导入，启动命令里的 `agentic_search.main:app` 也正是以此包路径定位应用。
 
 **理解示例**（教学示例）：平铺布局像把零件直接摊在桌上，随手拿容易拿错；src layout 像先把零件装进贴了标签的盒子（包），用时「按盒名取」，绝不会和桌上的散件混淆。
 
 **延伸阅读**：[Python 打包指南 · src layout vs flat layout](https://packaging.python.org/en/latest/discussions/src-layout-vs-flat-layout/)
 
 ---
 
 ## 数据存储
 
-**定义**：本项目数据的持久化方案——所有运行时数据（L1/L2 记忆、论文章节正文）统一存入一个本地 MongoDB 数据库。
+**定义**：本项目数据的持久化方案——所有运行时数据（L1/L2 记忆、论文文本）统一存入一个本地 MongoDB 数据库。
 
-**为什么需要**：上传的 PDF、切出的章节、积累的记忆都需要持久化，否则进程一停数据就丢。MongoDB 把数据集中在一个数据库、按集合归类，统一管理读写与一致性，应用代码不必操心存储细节。
+**为什么需要**：上传的 PDF、提取的文本、积累的记忆都需要持久化，否则进程一停数据就丢。MongoDB 把数据集中在一个数据库、按集合归类，统一管理读写与一致性，应用代码不必操心存储细节。
 
-**本项目用法**：数据库名为 `agentic_search`（地址 `localhost:27017`，配置项 `mongo_uri` / `mongo_db` 在 `configs/config.py`），其下两个集合各司其职——`memories` 集合存放 TMT 记忆数据（L1/L2，文档结构 `{ _id, level, content, timestamp, session_id }`）；`documents` 集合存放 pymupdf 切好的论文章节（文档结构 `{ _id, doc_id, filename, sections, uploaded_at }`，`sections` 是 `{section_id, title, level, text}` 章节数组）。上传流程把 PDF 经 pymupdf 提取为章节后直接写入 `documents` 集合（原 PDF 不落盘）；Agent 按需经 `read_section`/`read_document` 从 `documents` 读取某一章或全文；记忆则随时读写 `memories` 集合。原 `backend/data/` 目录已整体删除，项目零文件系统依赖。
+**本项目用法**：数据库名为 `agentic_search`（地址 `localhost:27017`，配置项 `mongo_uri` / `mongo_db` 在 `configs/config.py`），其下两个集合各司其职——`memories` 集合存放 TMT 记忆数据（L1/L2，文档结构 `{ _id, level, content, timestamp, session_id }`）；`documents` 集合存放 pymupdf 提取的论文文本（文档结构 `{ _id, doc_id, filename, text, uploaded_at }`，`text` 是完整纯文本）。上传流程把 PDF 经 pymupdf 提取为纯文本后直接写入 `documents` 集合（原 PDF 不落盘）；Agent 按需经 `read_paper`/`search_papers` 从 `documents` 按行号取片段或定位关键词；记忆则随时读写 `memories` 集合。原 `backend/data/` 目录已整体删除，项目零文件系统依赖。
 
 **理解示例**（教学示例）：
 ```
 MongoDB: agentic_search（localhost:27017，唯一数据存储）
 ├── memories      # 记忆区：L1/L2 文档
 │   └── { _id, level, content, timestamp, session_id }
-└── documents     # 文档区：论文章节
-    └── { _id, doc_id, filename, sections, uploaded_at }
+└── documents     # 文档区：论文文本
+    └── { _id, doc_id, filename, text, uploaded_at }
 ```
 集合分明，就像档案柜里「记忆抽屉」与「论文抽屉」各居其位，用 Compass 或 PyMongo 即可随时取阅。
 
 **延伸阅读**：详见 [模块 1](./01-Python文档工具.md) 与 [模块 4](./04-TMT记忆系统.md)，以及 [MongoDB 官方文档](https://www.mongodb.com/zh-cn/docs/manual/core/databases-and-collections/)
diff --git a/任务文档/项目概览.md b/任务文档/项目概览.md
index 877ac66..f47a87d 100644
--- a/任务文档/项目概览.md
+++ b/任务文档/项目概览.md
@@ -68,60 +68,60 @@ agentic-search/
 │   │   ├── configs/
 │   │   │   └── config.py           # 配置（从 .env 读：超时、MongoDB URI/库名、LLM 模型名）
 │   │   ├── api/
 │   │   │   ├── routes.py           # 路由：query/ingest/documents/consolidate
 │   │   │   └── schemas.py          # 请求/响应 Pydantic 模型
 │   │   ├── agents/
 │   │   │   └── graph.py            # LangGraph ReAct agent（llm_call ⇄ tool_node）+ 4 工具 + 条件边
 │   │   ├── memory/
 │   │   │   └── store.py            # TMT 记忆（L1/L2 + MongoDB 存储）
 │   │   └── services/
-│   │       └── documents.py        # 文档工具（parse_pdf 切章节 / list / read / read_section）
+│   │       └── documents.py        # 文档工具（parse_pdf 转纯文本 / list / read_document）
 │   │   # 数据不落盘（backend/data/ 已删除），全部存入 MongoDB：
 │   │   #   · agentic_search.memories  → L1/L2 记忆
-│   │   #   · agentic_search.documents → 论文章节正文（sections 数组）
+│   │   #   · agentic_search.documents → 论文完整文本
 │   └── tests/
 │       ├── test_documents.py
 │       ├── test_graph.py
 │       └── test_memory.py
 │
 ├── frontend/
 │   ├── index.html                # 页面结构（原生 HTML，无样式）
 │   └── app.js                    # 逻辑（fetch + DOM 操作）
 │
 └── AGENTS.md
 ```
 
 **各层职责一览**（架构中的位置）：
 
 - `main.py` — 瘦入口，只创建 app、挂路由、配 CORS，不含业务逻辑。
 - `configs/config.py` — 配置层，从 `.env` 读 LLM 模型名、超时、MongoDB URI 与库名。
 - `api/` — HTTP 层（`routes.py` 路由 + `schemas.py` 请求/响应模型），不碰 LLM/文档细节。
-- `agents/graph.py` — LangGraph ReAct agent：`llm_call ⇄ tool_node` 循环，条件边 `should_continue` 控制继续/终止，LLM 经 `bind_tools` 绑定 4 个论文导航工具（`list_papers`/`list_sections`/`read_section`/`search_sections`）。
-- `services/documents.py` — 文档工具（`parse_pdf` 用 `get_text("dict")` 切章节 / `list_documents` / `read_document` / `read_section`）。
+- `agents/graph.py` — LangGraph ReAct agent：`llm_call ⇄ tool_node` 循环，条件边 `should_continue` 控制继续/终止，LLM 经 `bind_tools` 绑定 4 个论文导航工具（`list_papers`/`read_paper`/`search_papers`/`extract_abstract`）。
+- `services/documents.py` — 文档工具（`parse_pdf` 用 `get_text("text")` 转纯文本 / `list_documents` / `read_document`）。
 - `memory/store.py` — TMT 记忆（L1 事实提取 + L2 会话整合 + MongoDB 存储）。
 
 系统直接运行，不需要额外的运行时环境。
 
 ## 模块设计
 
 项目按职责拆分为 3 个模块。每个模块都有明确的可验证产出。
 
 ### M1：Python AI 后端
 
 **学习目标**：Python 包化布局、文档处理、LangGraph ReAct agent（工具调用 + 条件边）、FastAPI 分层 API、装饰器（decorator）。
 
 内容：
 
 1. **pyproject.toml + 包化布局** — `uv sync` 后即可 `from agentic_search.agents.graph import ...`。
-2. **services/documents.py** — `parse_pdf()` 用 pymupdf 的 `get_text("dict")` 把 PDF 切成 `{section_id, title, level, text}` 章节后存入 MongoDB、`list_documents()` 查询集合、`read_document(doc_id)` 拼全文、`read_section(doc_id, section_id)` 读某一章。
-3. **agents/graph.py** — LangGraph ReAct agent：`llm_call ⇄ tool_node` 循环 + 条件边，LLM 经 `bind_tools` 自主调 `list_papers`/`list_sections`/`read_section`/`search_sections` 探索论文。
+2. **services/documents.py** — `parse_pdf()` 用 pymupdf 的 `get_text("text")` 把 PDF 转为完整纯文本后存入 MongoDB、`list_documents()` 查询集合、`read_document(doc_id)` 读完整文档。
+3. **agents/graph.py** — LangGraph ReAct agent：`llm_call ⇄ tool_node` 循环 + 条件边，LLM 经 `bind_tools` 自主调 `list_papers`/`read_paper`/`search_papers`/`extract_abstract` 探索论文。
 4. **api/routes.py + api/schemas.py** — 4 个端点 + Pydantic 请求/响应模型。
 5. **main.py** — 创建 app，挂载 routes，配置 CORS。
 6. **pytest 测试**（`test_documents` / `test_graph`）。
 
 **产出**：包化后端，包含文档工具（PDF 提取纯文本）+ LangGraph Agent 工作流 + 4 个 API 端点。
 
 ### M2：HTML 前端
 
 **学习目标**：HTML 结构、fetch API、DOM 操作、流式响应。
 
@@ -186,40 +186,40 @@ while (true) {
 
 ### 上传 PDF 流程
 
 ```
 浏览器 app.js
   │ FormData + fetch POST /api/ingest
   ▼
 FastAPI api/routes.py
   │ 接收 PDF（内存中，不落盘）
   ▼
-pymupdf get_text("dict") → 章节数组（内存中处理，不落盘）
+pymupdf get_text("text") → 完整纯文本（内存中处理，不落盘）
   │
   ▼
-存入 MongoDB documents 集合 {doc_id, filename, sections: 章节数组, uploaded_at}
+存入 MongoDB documents 集合 {doc_id, filename, text: 完整纯文本, uploaded_at}
   │ 返回 {doc_id, filename}
   ▼
 浏览器收到响应，更新文档列表
 ```
 
 ### 提问流程
 
 ```
 浏览器 app.js
   │ fetch POST /api/query {question}
   ▼
 FastAPI api/routes.py → StreamingResponse
   ▼
 LangGraph ReAct agent
   ├─ retrieve_memory: 查 L1/L2 记忆（M3 加入）
-  ├─ [ llm_call ⇄ tool_node ]：LLM 自主调 list_papers / list_sections / read_section / search_sections 探索论文
+  ├─ [ llm_call ⇄ tool_node ]：LLM 自主调 list_papers / read_paper / search_papers / extract_abstract 探索论文
   └─ store_memory: 提取事实存入记忆（M3 加入）
   │ 流式输出 tokens
   ▼
 浏览器 ReadableStream 逐块显示
 ```
 
 ### 记忆流（M3）
 
 ```
 L1（每轮对话后自动触发）：
@@ -236,26 +236,26 @@ L2（前端手动点击按钮触发，便于教学测试与展示）：
 ```
 
 **L2 触发机制说明**：TiMem 生产实现中（源码 `services/session_memory_scanner.py`），L2 由 `SessionMemoryScanner` 定期扫描，当会话**最后一次交互超过配置超时**（默认 `interaction_timeout_minutes=10`，即 10 分钟无交互）时视为会话已结束并触发 L2 生成。但教学项目为了便于测试和演示，不等待 10 分钟空闲，改为**前端手动点击「整合会话记忆」按钮**（`POST /api/consolidate`）即时触发。TiMem 的空闲超时原理仍作为背景概念讲解。
 
 ## 技术栈
 
 | 技术 | 角色 | 文件 |
 |------|------|------|
 | Python 3.11+ | 文档工具 + Agent + 记忆 | backend/ |
 | uv | Python 包管理 + 可安装包 | pyproject.toml |
-| pymupdf | PDF → 章节结构化提取（`get_text("dict")`） | services/documents.py |
+| pymupdf | PDF → 纯文本提取（`get_text("text")`） | services/documents.py |
 | LangChain | LLM + 工具调用协议（`init_chat_model`/`bind_tools`） | agents/graph.py, agents/tools.py |
 | LangGraph | ReAct agent 图（`ToolNode` + 条件边） | agents/graph.py |
 | FastAPI + uvicorn | Python API 服务（ASGI） | api/routes.py, main.py |
 | Pydantic | 请求/响应数据模型 | api/schemas.py |
-| MongoDB | 记忆数据 + 论文章节文档存储 | agentic_search（localhost:27017） |
+| MongoDB | 记忆数据 + 论文文档存储 | agentic_search（localhost:27017） |
 | PyMongo | MongoDB Python 驱动（同步） | memory/store.py, services/documents.py |
 | MongoDB Compass | 数据库可视化工具（教学调试） | — |
 | pytest | Python 测试 | tests/ |
 | 原生 HTML + JS（fetch） | 前端界面与 AJAX 通信 | frontend/ |
 
 ## 验证标准
 
 每个模块都有可执行命令验证。后端启动后（`cd backend && uv sync && uv run uvicorn agentic_search.main:app --reload --port 8000`）：
 
 | 模块 | 标志（可验证） |
